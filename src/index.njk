---
layout: base.njk
title: Home
pagination:
  data: collections.posts
  size: 10
  alias: posts
  reverse: true
---

<section class="page-hero">
    <div class="hero-copy">
        <p class="hero-kicker">Signal Log</p>
        <h1 class="hero-title">Cornucopica of Corbs</h1>
        <p class="hero-subtitle">Experiments, releases, and quick hits from the edge of product, code, and AI.</p>
    </div>
    <div class="hero-panel">
        <p class="panel-label">Archive Size</p>
        <p class="panel-value">{{ collections.posts | length }}</p>
        <p class="panel-caption">posts and counting</p>
    </div>
</section>

<ul class="post-list">
{% for post in posts %}
    {% include "components/post-item.njk" %}
{% endfor %}
</ul>

<div class="pagination">
    {% if pagination.href.previous %}
        <a href="{{ pagination.href.previous }}"><<< Newer</a>
    {% else %}
        <span></span>
    {% endif %}

    {% if pagination.href.next %}
        <a href="{{ pagination.href.next }}">Older >>></a>
    {% else %}
        <span></span>
    {% endif %}
</div>
