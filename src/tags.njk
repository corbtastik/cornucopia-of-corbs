---
title: "Tags"
layout: base.njk
description: "Browse posts by topic."
---

<section class="page-hero compact">
    <div class="hero-copy">
        <p class="hero-kicker">Index</p>
        <h1 class="hero-title">Themes, threads, and deep dives.</h1>
        <p class="hero-subtitle">Jump into a topic and follow the trail.</p>
    </div>
    <div class="hero-panel">
        <p class="panel-label">Tag count</p>
        <p class="panel-value">{{ collections | length }}</p>
        <p class="panel-caption">topics</p>
    </div>
</section>

<!-- 1. Tag Cloud / Navigation -->
<div class="tag-list">
{% for tag, posts in collections %}
  {% if tag != "all" and tag != "posts" %}
    <a href="#{{ tag }}" class="tag">
        #{{ tag }} <span>{{ posts.length }}</span>
    </a>
  {% endif %}
{% endfor %}
</div>

<hr>

<!-- 2. The Sections -->
{% for tag, posts in collections %}
  {% if tag != "all" and tag != "posts" %}
    
    <section id="{{ tag }}">
      <!-- Header -->
      <h2>#{{ tag }}</h2>

      <!-- Replaced custom HTML with the standard component -->
      <ul class="post-list">
        {% for post in posts | reverse %}
          
          {# Pass hide_tags to keep the list clean, since we are already inside a specific tag section #}
          {% set hide_tags = true %}
          {% include "components/post-item.njk" %}
        
        {% endfor %}
      </ul>
    </section>

  {% endif %}
{% endfor %}
