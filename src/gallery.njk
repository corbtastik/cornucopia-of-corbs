---
layout: base.njk
title: Photography
description: "A collection of photos."
mainClass: "wide-layout" 
---
  
<h1 style="margin-bottom: 2rem;">{{ title }}</h1>

<div class="gallery-container">
  <div class="masonry-grid" id="gallery-grid">
    
    {% for photo in gallery %}
      <div class="gallery-item" style="--aspect-ratio: {{ photo.aspectRatio }}">
        
        <img 
          src="{{ photo.thumb }}" 
          data-full-src="{{ photo.src }}"
          alt="{{ photo.alt }}"
          width="{{ photo.width }}"
          height="{{ photo.height }}"
          loading="lazy"
          class="lightbox-trigger"
          data-caption="{{ photo.caption }}"
          data-source="{{ photo.source }}"
        />
        
        <div class="overlay">
          <span>{{ photo.source }}</span>
        </div>

      </div>
    {% endfor %}

  </div>
</div>